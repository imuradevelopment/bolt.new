# ===== BE .env.example (clean) =====

# ---------- Server ----------
PORT=4000
CORS_ORIGIN=http://localhost:3000

# ---------- Database (Postgres) ----------
# 単一ソースで管理（compose も含め全てこの値を参照）
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=llm_platform
POSTGRES_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}

# ---------- Auth ----------
# 十分長いランダム文字列に置き換えてください
JWT_SECRET=change-me-to-a-long-random-string

# ---------- LLM (Gemini) ----------
LLM_PROVIDER=gemini
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-2.5-pro

# 生成パラメータ（必須・フォールバックなし）
# Google AI Studio (Gemini 2.5 Pro) デフォルト寄り
LLM_MAX_TOKENS=65536
LLM_MAX_RESPONSE_SEGMENTS=0     # 継続生成を禁止（>0 で“続きを生成”許可）
LLM_TEMPERATURE=1

# サンプリング（任意・どちらか一方のみ推奨）
LLM_TOP_P=0.95
# LLM_TOP_K=40

# ---------- pgAdmin ----------
PGADMIN_DEFAULT_EMAIL=admin@example.com
# パスワードは DB_PASSWORD を流用（compose 側で参照）